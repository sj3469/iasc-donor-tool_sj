# Copy this file's contents into the Streamlit Cloud Secrets editor
# (App settings > Secrets) when deploying.
# Do NOT commit a real secrets.toml to the repo.

ANTHROPIC_API_KEY = "sk-ant-your-key-here"
